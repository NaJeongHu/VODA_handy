<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.voda_handy">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VODA_handy">
        <activity android:name=".PayActivity"></activity>
        <activity android:name=".ShoppingBasketActivity"></activity>
        <activity android:name=".ShoppingBasketEmptyActivity"></activity>
        <activity android:name=".DetailedMenuActivity"></activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".StoreListActivity" />
        <activity android:name=".MypageActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>